import RequiredResources from "../../Resource/Models/required.resources";

export class WeaponCalculator {
  public calculate(start, end): RequiredResources {
    // TODO
    return new RequiredResources();
  }
}
